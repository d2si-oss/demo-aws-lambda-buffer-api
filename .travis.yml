dist: trusty
sudo: required
language: node_js
node_js:
  - "7.6"
python:
  - "2.7"
git:
  depth: 10
cache:
  directories:
    - node_modules
before_install:
  - pip install --user awscli
install:
  - cd code/buffer && pip install --user -r requirements.txt
script:
  - cd code/buffer && script/on_change
